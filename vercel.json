{
  "rewrites": [{ "source": "/(.*)", "destination": "/frontend/dashboard.html" }]
}
```

Save, then push:
```
cd C:\Users\pritb\Desktop\guardian-ai
git add vercel.json
git commit -m "Simplify vercel config"
git push
```

Wait 30 seconds then check:
```
https://defi-risk-governance-agent.vercel.app